# pnpm 配置
# miniprogram-ci 内部使用 @babel/preset-env 编译 JS，其依赖 @babel/types
# 在 pnpm 严格隔离结构下无法被 worker 找到。用 public-hoist-pattern 仅提升
# babel 相关包到顶层，保留 miniprogram-ci 自身依赖树（如 less）不被破坏。
# 注意：不要用 shamefully-hoist=true，否则会把 less 从 miniprogram-ci 的
# node_modules 里移走，导致 require('less/lib/less') 深路径失效。
public-hoist-pattern[]=@babel/*
